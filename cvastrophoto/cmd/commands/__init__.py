# -*- coding: utf-8 -*-
from . import darklib, biaslib, flatslib, process, guide, apply, combine, measure, register, info, collection

ALL_COMMANDS = {
    'darklib': darklib,
    'biaslib': biaslib,
    'flatslib': flatslib,
    'collection': collection,
    'process': process,
    'guide': guide,
    'apply': apply,
    'combine': combine,
    'measure': measure,
    'register': register,
    'info': info,
}
