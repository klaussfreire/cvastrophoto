# -*- coding: utf-8 -*-
from . import darklib, biaslib, process, guide, apply, combine, measure

ALL_COMMANDS = {
    'darklib': darklib,
    'biaslib': biaslib,
    'process': process,
    'guide': guide,
    'apply': apply,
    'combine': combine,
    'measure': measure,
}
